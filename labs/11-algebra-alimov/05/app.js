(()=>{'use strict';const $=id=>document.getElementById(id),p=$('p'),n=$('n'),c=$('chart'),ctx=c.getContext('2d'),pOut=$('pOut'),nOut=$('nOut'),success=$('success'),freq=$('freq'),delta=$('delta');let seq=[];function rnd(prob){return Math.random()<prob?1:0}function make(N,prob){seq=Array.from({length:N},()=>rnd(prob));draw()}function add(k){const prob=+p.value;for(let i=0;i<k;i++)seq.push(rnd(prob));n.value=Math.min(2000,seq.length);draw()}function draw(){const prob=+p.value,N=seq.length;pOut.textContent=prob.toFixed(2).replace('.',',');nOut.textContent=N;let s=0,pts=[];seq.forEach((x,i)=>{s+=x;pts.push(s/(i+1))});success.textContent=s;const w=N?s/N:0;freq.textContent=N?w.toFixed(3).replace('.',','):'—';delta.textContent=N?Math.abs(w-prob).toFixed(3).replace('.',','):'—';const W=c.width,H=c.height,pad=42;ctx.clearRect(0,0,W,H);ctx.strokeStyle='#dfe4ee';ctx.lineWidth=1;for(let y=0;y<=4;y++){const yy=pad+(H-2*pad)*y/4;ctx.beginPath();ctx.moveTo(pad,yy);ctx.lineTo(W-pad,yy);ctx.stroke();ctx.fillStyle='#667085';ctx.fillText((1-y/4).toFixed(2),8,yy+4)}ctx.strokeStyle='#1d2433';ctx.lineWidth=2;const py=pad+(1-prob)*(H-2*pad);ctx.beginPath();ctx.moveTo(pad,py);ctx.lineTo(W-pad,py);ctx.stroke();if(pts.length){ctx.strokeStyle='#5b6cff';ctx.lineWidth=2;ctx.beginPath();pts.forEach((v,i)=>{const x=pad+(W-2*pad)*(pts.length===1?0:i/(pts.length-1)),y=pad+(1-v)*(H-2*pad);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()}ctx.fillStyle='#667085';ctx.fillText('1',pad,H-12);ctx.fillText(String(Math.max(1,N)),W-pad-25,H-12)}p.addEventListener('input',()=>make(+n.value,+p.value));n.addEventListener('input',()=>make(+n.value,+p.value));$('run').addEventListener('click',()=>make(+n.value,+p.value));$('add').addEventListener('click',()=>add(Math.min(100,2000-seq.length)));make(+n.value,+p.value)})();
