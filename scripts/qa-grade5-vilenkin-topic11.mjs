import fs from'node:fs';import path from'node:path';import vm from'node:vm';
const R=process.cwd();let checks=0;const ok=(c,m)=>{checks++;if(!c)throw Error(m)},p=x=>path.join(R,x),has=x=>fs.existsSync(p(x)),read=x=>fs.readFileSync(p(x),'utf8'),rel=(f,t)=>path.normalize(path.join(path.dirname(f),t)).replaceAll('\\','/');
const compile=(src,file)=>{try{new vm.Script(src,{filename:file});checks++}catch(e){throw Error(`${file}: syntax ${e.message}`)}};const run=(src,ctx,file)=>{try{new vm.Script(src,{filename:file}).runInContext(ctx);checks++}catch(e){throw Error(`${file}: ${e.message}`)}};
function scan(v,l,s=new Set()){if(v===undefined)throw Error(`${l}: undefined`);if(typeof v==='number')ok(Number.isFinite(v),`${l}: non-finite`);if(typeof v==='string'){ok(!/\b(?:NaN|Infinity|undefined)\b/.test(v),`${l}: forbidden token`);ok(!/Исправление:|Проверка показывает|\?\s*Проверка/.test(v),`${l}: editorial artifact`)}if(!v||typeof v!=='object'||s.has(v))return;s.add(v);Array.isArray(v)?v.forEach((x,i)=>scan(x,`${l}[${i}]`,s)):Object.entries(v).forEach(([k,x])=>scan(x,`${l}.${k}`,s))}
function tasks(a,l,n=1){ok(Array.isArray(a)&&a.length>=n,`${l}: <${n} tasks`);a.forEach((t,i)=>{ok(String(t?.text??t?.task??t??'').trim(),`${l}[${i}]: empty text`);if(t&&typeof t==='object')ok(String(t.answer??'').trim(),`${l}[${i}]: empty answer`)})}
function assess(b,l,score){ok(b?.variants?.length===6,`${l}: expected 6 variants`);ok(Number(b.maxScore)===score,`${l}: maxScore`);const ids=new Set(),sigs=new Set();b.variants.forEach(v=>{ids.add(String(v.id));tasks(v.tasks,`${l} v${v.id}`);ok(v.tasks.reduce((s,t)=>s+Number(t.points||0),0)===score,`${l} v${v.id}: score`);v.tasks.forEach(t=>ok(Number(t.points)>0,`${l} v${v.id}: points`));sigs.add(JSON.stringify(v.tasks.map(t=>t.text)))});ok(ids.size===6,`${l}: duplicate ids`);ok(sigs.size===6,`${l}: duplicate variants`)}
const titles=['Умножение десятичной дроби на натуральное число','Смысл запятой в результате умножения','Свойства умножения и рациональные вычисления','Текстовые задачи на умножение десятичной дроби','Деление десятичной дроби на натуральное число','Деление, когда в частном появляются нули','Проверка деления умножением','Текстовые задачи на деление на натуральное число','Умножение на 10, 100, 1000 и разрядный смысл','Умножение десятичной дроби на десятичную: модель','Алгоритм умножения десятичных дробей','Прикидка результата умножения','Текстовые задачи на умножение десятичных дробей','Подготовка к делению на десятичную дробь: равные частные','Алгоритм деления на десятичную дробь','Прикидка и проверка результата деления','Текстовые задачи на деление на десятичную дробь','Выражения и уравнения со всеми действиями над десятичными дробями','Калькулятор как инструмент проверки и исследования','Обобщение действий с десятичными дробями и диагностика'];
const topic='topics/5-math-vilenkin/11.html',cf='content/5-math-vilenkin/11.js';ok(has(topic),`${topic}: missing`);ok(has(cf),`${cf}: missing`);const th=read(topic);ok(/viewport/i.test(th),`${topic}: viewport`);['../../content/registry.js','../../content/5-math-vilenkin/11.js','../../assessments/topic-links.js','../../lessons/topic-links.js'].forEach(x=>ok(th.includes(x),`${topic}: missing ${x}`));
let reg;const cc=vm.createContext({KTP_REGISTER_CONTENT:(id,data)=>reg={id,data},window:{},console});compile(read(cf),cf);run(read(cf),cc,cf);ok(reg?.id==='5-math-vilenkin::10',`${cf}: id`);scan(reg.data,cf);const c=reg.data;ok(c.meta?.title==='Умножение и деление десятичных дробей',`${cf}: title`);ok(c.objectives?.length>=4&&c.expectedResults?.length>=12&&c.prerequisites?.length>=5,`${cf}: goals`);tasks(c.prerequisiteCheck,`${cf} prerequisiteCheck`,4);ok(c.map?.length>=13&&c.theory?.length>=13&&c.examples?.length>=8&&c.mistakes?.length>=7,`${cf}: core blocks`);c.examples.forEach((e,i)=>['problem','idea','solution','check','answer'].forEach(k=>ok(String(e[k]??'').trim(),`${cf}: example ${i+1} ${k}`)));['basic','standard','transfer','challenge'].forEach(k=>tasks(c.practice?.[k],`${cf} ${k}`));tasks(c.diagnostic,`${cf} diagnostic`,5);tasks(c.homework?.required,`${cf} hw`,6);tasks(c.homework?.optional,`${cf} hw optional`,2);ok(c.summary?.length>=8,`${cf}: summary`);ok(c.lab?.enabled!==true,`${cf}: unexpected lab`);const src=(c.source?.paragraphs||[]).join(' ');ok(String(c.source?.textbook||'').includes('Виленкин')&&src.includes('45–48')&&src.includes('49'),`${cf}: source`);
ok(c.practice.standard[0].answer==='0,84.'&&c.practice.standard[1].answer==='4,8.'&&c.practice.standard[2].answer==='160.'&&c.practice.standard[3].answer==='x=7.',`${cf}: standard math`);ok(c.practice.transfer[0].answer==='9 у. е.'&&c.practice.transfer[1].answer==='50,6 км/ч.'&&c.practice.transfer[3].answer==='1,92.',`${cf}: transfer math`);ok(c.practice.challenge[0].answer==='4,6.'&&c.practice.challenge[1].answer==='x=4.',`${cf}: challenge math`);ok(c.diagnostic[0].answer==='10,2.'&&c.diagnostic[1].answer==='12,6.'&&c.diagnostic[3].answer==='x=8.'&&c.diagnostic[4].answer==='7,8 у. е.',`${cf}: diagnostic math`);
const futureRe=/(транспортир|измерьте\s+угол|постройте\s+угол|градусн)/i;ok(!futureRe.test(JSON.stringify({practice:c.practice,diagnostic:c.diagnostic,homework:c.homework})),`${cf}: future-topic task`);
const base='lessons/5-math-vilenkin/11',sf=`${base}/series.js`,df=`${base}/data.js`;[`${base}/index.html`,sf,df].forEach(f=>ok(has(f),`${f}: missing`));compile(read(sf),sf);compile(read(df),df);const lc=vm.createContext({window:{},console,Math,Number});run(read(sf),lc,sf);run(read(df),lc,df);const S=lc.window.KTP_LESSON_SERIES;scan(S,base);ok(S?.meta?.rowId==='5-math-vilenkin'&&Number(S.meta.topicIndex)===10&&Number(S.meta.topicNumber)===11,`${base}: metadata`);ok(Number(S.meta.totalLessons)===20&&Number(S.meta.courseLessonStart)===134&&Number(S.meta.courseLessonEnd)===153&&Number(S.meta.courseTotal)===170&&Number(S.meta.globalStart)===134,`${base}: numbering`);ok(S.meta.plannedWeeks==='27–31'&&S.lessons?.length===20,`${base}: span`);ok(!futureRe.test(JSON.stringify(S.lessons)),`${base}: future-topic task`);let w=0;S.lessons.forEach((l,i)=>{const id=String(i+1).padStart(2,'0'),f=`${base}/${id}.html`,g=134+i;ok(has(f),`${f}: missing`);const h=read(f);ok(/viewport/i.test(h),`${f}: viewport`);['series.js','data.js','../../lesson-page.js','../../global-numbering.js'].forEach(x=>ok(h.includes(x),`${f}: ${x}`));ok(String(l.id)===id&&Number(l.number)===i+1&&Number(l.globalNumber)===g,`${id}: numbering`);ok(l.title===titles[i],`${id}: title`);const ww=Number(l.week);ok(Number.isInteger(ww)&&ww>=27&&ww<=31&&ww>=w,`${id}: week`);w=ww;ok(l.objectives?.length>=3&&l.prerequisites?.length>=2&&l.theory?.length>=2&&l.examples?.length>=2&&l.mistakes?.length>=3,`${id}: blocks`);l.examples.forEach((e,j)=>['problem','idea','solution','check','answer'].forEach(k=>ok(String(e[k]??'').trim(),`${id}: ex${j+1} ${k}`)));tasks(l.practice,`${id} practice`,8);tasks(l.homework?.required,`${id} hw`,6);tasks(l.homework?.optional,`${id} hw opt`,2);assess(l.independent,`${id} independent`,8);assess(l.control,`${id} control`,12);ok(l.summary?.length>=3&&String(l.source?.section||'').trim(),`${id}: tail blocks`)});ok(S.lessons[19].milestone==='Итог темы','20: milestone');
const ab='assessments/5-math-vilenkin/11';['independent.html','control.html','data.js'].forEach(n=>ok(has(`${ab}/${n}`),`${ab}/${n}: missing`));compile(read(`${ab}/data.js`),`${ab}/data.js`);const ac=vm.createContext({window:{},console,Math,Number});run(read(`${ab}/data.js`),ac,`${ab}/data.js`);const A=ac.window.KTP_ASSESSMENT_DATA;scan(A,ab);ok(A?.meta?.rowId==='5-math-vilenkin'&&Number(A?.meta?.grade)===5&&A.topic?.title==='Умножение и деление десятичных дробей',`${ab}: metadata`);assess(A.topic.independent,`${ab} independent`,14);assess(A.topic.control,`${ab} control`,20);for(const skill of['Умножение десятичных дробей','Деление на десятичную дробь','Прикидка','Анализ ошибки'])ok(A.topic.independent.variants.every(v=>v.tasks.some(t=>t.skill===skill)),`${ab}: independent ${skill}`);for(const skill of['Умножение','Деление','Текстовая задача','Проверка разумности'])ok(A.topic.control.variants.every(v=>v.tasks.some(t=>t.skill===skill)),`${ab}: control ${skill}`);
ok(read('lessons/topic-links.js').includes('10:{count:20'),`lesson topic link`);const atl=read('assessments/topic-links.js');ok(Number(atl.match(/'5-math-vilenkin':\{min:0,max:(\d+)\}/)?.[1])>=10,`assessment topic link`);
const html=[topic,`${base}/index.html`,...Array.from({length:20},(_,i)=>`${base}/${String(i+1).padStart(2,'0')}.html`),`${ab}/independent.html`,`${ab}/control.html`];for(const f of html)for(const m of read(f).matchAll(/(?:href|src)=["']([^"']+)["']/g)){const t=m[1];if(!t||t.startsWith('#')||/^(?:https?:|mailto:|data:|javascript:|\/\/)/.test(t))continue;const q=t.split(/[?#]/)[0];if(q)ok(has(rel(f,q)),`${f}: broken ${t}`)}
console.log(`Grade 5 Vilenkin topic 11 QA passed: ${checks} checks, 20 lessons, 240 lesson assessment variants, 12 thematic variants.`);
